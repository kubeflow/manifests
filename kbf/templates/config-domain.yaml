apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kbf.fullname" . }}-config-domain
  labels:
    app.kubernetes.io/component: controller
  {{- include "kbf.labels" . | nindent 4 }}
  annotations:
    knative.dev/example-checksum: 26c09de5
data:
  _example: {{ .Values.configDomain.Example | toYaml | indent 1 }}