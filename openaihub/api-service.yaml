apiVersion: v1
kind: Service
metadata:
  name: openaihub-api
  namespace: kubeflow
  labels:
    service: openaihub-api
    environment: dev
  annotations:
    getambassador.io/config: |-
      ---
      apiVersion: ambassador/v0
      kind:  Mapping
      name: openaihub-api
      prefix: /apis
      rewrite: /apis
      timeout_ms: 300000
      service: openaihub-api.kubeflow
      use_websocket: true
spec:
  type: NodePort
  ports:
  - name: openaihub-api
    port: 80
    targetPort: 8080
  selector:
    service: openaihub-api