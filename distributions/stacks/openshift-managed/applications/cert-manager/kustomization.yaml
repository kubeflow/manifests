apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - subscription.yaml
  - namespace.yaml
  - cert-manager.yaml
  - cluster-issuer.yaml # Requires successful deployment of CertManager resource to create CDRs
