apiVersion: apps/v1
kind: Deployment
metadata:
  name: kuberay-operator
  namespace: kubeflow
spec:
  template:
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: kuberay-operator
        securityContext:
          seccompProfile:
            type: RuntimeDefault
