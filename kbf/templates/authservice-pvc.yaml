apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "kbf.fullname" . }}-authservice-pvc
  labels:
  {{- include "kbf.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.authservicePvc.storageRequest | quote }}