apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "kbf.fullname" . }}-webhook-pdb
  labels:
    app.kubernetes.io/component: webhook
  {{- include "kbf.labels" . | nindent 4 }}
spec:
  minAvailable: 80%
  selector:
    matchLabels:
      app: webhook