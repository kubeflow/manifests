apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: deployment
spec:
  template:
    spec:
      containers:
      - name: container
        args:
        - --alsologtostderr
        - -v=1
        - --enable-gang-scheduling
