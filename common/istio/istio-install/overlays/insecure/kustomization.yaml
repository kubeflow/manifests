apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../oauth2-proxy

# TODO disable the istio-cni injection with a minimal patch.
# Probably only the istio-sidecar injection configmap and the istiod deployment must be patched
