apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# TODO: how do we name this kustomization? the oauth2-proxy name was taken from
# the previous one
resources:
# Using kserve overlay because it's also used in example installation.
- ../../upstream/centraldashboard/overlays/kserve
- ../../upstream/poddefaults-webhooks/overlays/cert-manager
- ../../upstream/profile-controller/overlays/kubeflow-pss

