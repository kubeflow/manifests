apiVersion: v1
kind: Service
metadata:
  name: kepler
  namespace: kubeflow-monitoring-system
  labels:
    app.kubernetes.io/name: kepler
spec:
  ports:
  - name: http
    port: 9102
    targetPort: 9102
  selector:
    app.kubernetes.io/name: kepler
