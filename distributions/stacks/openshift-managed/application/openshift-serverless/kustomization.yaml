---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - subscription.yaml
  - knative-serving.yaml # Requires to wait for operator to deploy to create knative-serving namespace
  - knative-eventing.yaml # Requires to wait for operator to deploy to create knative-eventing namespace
