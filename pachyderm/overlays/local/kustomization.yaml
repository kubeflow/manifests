apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base
configMapGenerator:
- name: parameters
  behavior: merge
  env: params.env
resources:
- role.yaml
- role-binding.yaml
configurations:
- params.yaml