{{- if and (not .Values.externalDatabase.existingSecret) (not .Values.mysql.enabled) (not .Values.postgresql.enabled) }}
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: {{ include "kubeflow-pipelines.namespace" . }}
{{- if not (or (eq .Values.env.platform "gcp") ) }}
  labels:
    {{- include "kubeflow-pipelines.labels" . | nindent 4 }}
{{- end }}
{{- if or (eq .Values.env.platform "aws") (eq .Values.env.platform "gcp") (eq .Values.env.platform "azure") }}
type: Opaque
{{- else }}
stringData:
  username: {{ .Values.externalDatabase.username | quote }}
  password: {{ .Values.externalDatabase.password | quote }}
{{- end }}
{{- end }}
