apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubeflow-system

resources:
- ../upstream/overlays/manager

patches:
- path: jobset-pss-fix.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: jobset-controller-manager