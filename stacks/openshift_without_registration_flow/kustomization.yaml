apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubeflow
resources:
  - ../openshift
configMapGenerator:
  - name: kubeflow-config
    behavior: merge
    envs:
      - ./config/params.env
