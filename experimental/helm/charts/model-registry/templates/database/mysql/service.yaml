{{- if .Values.database.mysql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: model-registry-db
  labels:
    component: db
spec:
  type: ClusterIP
  ports:
  - name: dbapi
    port: 3306
    protocol: TCP
  selector:
    component: db
{{- end }} 