apiVersion: v1
kind: Service
metadata:
  labels:
    app: etcd
  name: etcd-headless
spec:
  clusterIP: None
  sessionAffinity: None
  ports:
    - name: peer-port
      port: 2380
      protocol: TCP
      targetPort: 2380
  selector:
    app: etcd