*.idea
.vscode
.DS_Store

**/*.pyc

# Swap
[._]*.s[a-v][a-z]
!*.svg  # comment out if you don't need vector files
[._]*.sw[a-p]
[._]s[a-rt-v][a-z]
[._]ss[a-gi-z]
[._]sw[a-p]


# Scanning reports of trivy
image_lists/*

# Helm dependency archives. Charts pin their dependencies in Chart.yaml and
# Chart.lock; the archive itself is a build artifact of helm dependency build.
**/helm/charts/*.tgz