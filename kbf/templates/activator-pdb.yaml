apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "kbf.fullname" . }}-activator-pdb
  labels:
    app.kubernetes.io/component: activator
  {{- include "kbf.labels" . | nindent 4 }}
spec:
  minAvailable: 80%
  selector:
    matchLabels:
      app: activator