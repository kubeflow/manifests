- op: remove
  path: /stringData/DEX_USER_PASSWORD
{% for user in auth.dex.staticPasswords %}
- op: add
  path: /stringData/DEX_PASSWORD_{{ user.username | slugify | upper }}
  value: "{{ user.hash }}"
{% endfor %}
