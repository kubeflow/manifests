apiVersion: v1
kind: Service
metadata:
  name: openaihub-ui
  namespace: kubeflow
  labels:
    service: openaihub-ui
  annotations:
    getambassador.io/config: |-
      ---
      apiVersion: ambassador/v0
      kind:  Mapping
      name: openaihub-ui
      prefix: /aiops
      rewrite: /aiops
      timeout_ms: 300000
      service: openaihub-ui.kubeflow
      use_websocket: true
      ---
      apiVersion: ambassador/v0
      kind:  Mapping
      name: openaihub-ui-static
      prefix: /static
      rewrite: /static
      timeout_ms: 300000
      service: openaihub-ui.kubeflow
      use_websocket: true
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  type: NodePort
  selector:
    service: openaihub-ui