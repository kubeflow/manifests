apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: central-dashboard
spec:
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/{{ istio.gateway.namespace }}/sa/{{ istio.gateway.serviceAccount }}
