apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: profiles
    app.kubernetes.io/name: profiles
    kustomize.component: profiles
  name: profiles-kfam
spec:
  ports:
  - port: 8081
  selector:
    app.kubernetes.io/component: profiles
    app.kubernetes.io/name: profiles
    kustomize.component: profiles
