apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
   - ../../../../../apps/kfp-tekton/upstream/env/plain
   - ../../../../../apps/kfp-tekton/upstream/third-party/tekton-custom-task
   - ../../../../../apps/kfp-tekton/upstream/third-party/application/cluster-scoped
   - virtual-service.yaml

patchesStrategicMerge:
   - removesidemenu.yaml

patchesJson6902:
   - path: ../../patch_sidecar.yaml
     target:
        group: apps
        version: v1
        kind: Deployment
        name: metadata-envoy-deployment
