apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kbf.fullname" . }}-domainmapping-webhook-certs
  labels:
    app.kubernetes.io/component: domain-mapping
  {{- include "kbf.labels" . | nindent 4 }}