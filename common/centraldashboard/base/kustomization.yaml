apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../base_v3
patchesStrategicMerge:
- deployment_patch.yaml
namespace: kubeflow
commonLabels:
  kustomize.component: centraldashboard
images:
- name: gcr.io/kubeflow-images-public/centraldashboard
  newName: gcr.io/kubeflow-images-public/centraldashboard
  newTag: vmaster-gf39279c0
configMapGenerator:
- envs:
  - params.env
  name: parameters
generatorOptions:
  disableNameSuffixHash: true
vars:
# TODO(jlewi): This is commented out because when we combine applicaitons
# we can't have multiple vars with the same name
#- fieldref:
#    fieldPath: metadata.namespace
# name: namespace
#  objref:
#    apiVersion: v1
#    kind: Service
#    name: centraldashboard
# TODO(jlewi): This is commented out because when we combine applicaitons
# we can't have multiple vars with the same name    
#- fieldref:
#    fieldPath: data.clusterDomain
#  name: clusterDomain
#  objref:
#    apiVersion: v1
#    kind: ConfigMap
#    name: parameters
# We shouldn't need these vars anymore because we set the environment variables from a global configmap
#- fieldref:
#    fieldPath: data.userid-header
#  name: userid-header
#  objref:
#    apiVersion: v1
#    kind: ConfigMap
#    name: parameters
#- fieldref:
#    fieldPath: data.userid-prefix
#  name: userid-prefix
#  objref:
#    apiVersion: v1
#    kind: ConfigMap
#    name: parameters
configurations:
- params.yaml
