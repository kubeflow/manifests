# The config package defines global configuration for
# kubeflow. In particular it is used to generate
# a configmap that contains parameters which should
# be reused across applications.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubeflow
configMapGenerator:
- envs:
  - params.env
  name: kubeflow-config
