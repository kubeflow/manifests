{{- if .Values.cacheDeployer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache-deployer-deployment
  namespace: {{ include "kubeflow-pipelines.namespace" . }}
  labels:
    {{- include "kubeflow-pipelines.labels" . | nindent 4 }}
    app: cache-deployer
spec:
  replicas: {{ .Values.cacheDeployer.replicas }}
  selector:
    matchLabels:
      {{- include "kubeflow-pipelines.cacheDeployerSelectorLabels" . | nindent 6 }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        {{- include "kubeflow-pipelines.labels" . | nindent 8 }}
        app: cache-deployer
    spec:
      serviceAccountName: kubeflow-pipelines-cache-deployer-sa
      containers:
      - name: main
        image: {{ include "kubeflow-pipelines.image" (dict "repository" .Values.cacheDeployer.image.repository "tag" .Values.cacheDeployer.image.tag "context" .) }}
        imagePullPolicy: Always
        env:
        - name: NAMESPACE_TO_WATCH
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
      restartPolicy: Always
{{- end }}
