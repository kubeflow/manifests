apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: role
rules:
- apiGroups:
  - kubeflow.org
  resources:
  - podpresets
  verbs:
  - get
  - watch
  - list
  - update
  - create
  - patch
  - delete
