apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
vars:
- name: namespace
  objref:
    kind: ConfigMap
    name: parameters
    apiVersion: v1
  fieldref:
    fieldpath: data.namespace
resources:
- role.yaml
- role-binding.yaml
configurations:
- params.yaml