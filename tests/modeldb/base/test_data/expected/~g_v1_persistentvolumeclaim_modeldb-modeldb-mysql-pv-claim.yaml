apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: modeldb
    kustomize.component: modeldb
  name: modeldb-modeldb-mysql-pv-claim
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
