apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  ports:
  - port: 3306 # We cannot have name: mysql here, because some requests through istio fail with it.
    protocol: TCP
    targetPort: 3306
  selector:
    app: mysql
